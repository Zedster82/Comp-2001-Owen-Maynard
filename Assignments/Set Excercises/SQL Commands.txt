CREATE SCHEMA CW1




CREATE TABLE CW1.[User] (
    user_id INTEGER PRIMARY KEY IDENTITY(1,1),
    first_name VARCHAR(40),
    surname VARCHAR(40),
    location VARCHAR(40),
    about_me VARCHAR(500),
    email VARCHAR(80),
    units VARCHAR(10) CHECK (units IN ('imperial', 'metric')),
    activity_time_preference VARCHAR(15),
    weight FLOAT,
    height FLOAT,
    birthday DATE,
    language VARCHAR(30),
    password VARCHAR(40),
    account_type VARCHAR(15),
    is_archive BIT
);



CREATE TABLE CW1.[Favourite_Activities] (
	FOREIGN KEY (activity_id) REFERENCES CW1.Activity(activity_id),
	FOREIGN KEY (user_id) REFERENCES CW1.User(user_id)
);



CREATE TABLE CW1.[Activity] (
	activity_id INTEGER PRIMARY KEY IDENTITY(1,1),
	activity_type VARCHAR(30)
);


CREATE TABLE CW1.[Follow_List] (
	FOREIGN KEY (user_id) REFERENCES CW1.User(user_id),
	follow_id INTEGER






ALTER TABLE CW1.[Archived_User]
ADD CONSTRAINT user_id FOREIGN KEY([user_id]) 
    REFERENCES CW1.[User]([user_id])



INSERT INTO [User] (
	[user_id],
	[firstname],
	[secondname],
	[email],
	[password]
)
VALUES
	(1, 'Grace', 'Hopper', 'grace@plymouth.ac.uk', 'ISAD123!'),
	(2, 'Tim', 'Berners-Lee', 'tim@plymouth.ac.uk', 'COMP2001!'),
	(3, 'Ada', 'Lovelace', 'ada@plymouth.ac.uk', 'insecurePassword');





